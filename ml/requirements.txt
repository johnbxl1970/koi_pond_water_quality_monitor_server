fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
psycopg[binary]==3.2.3
# CUID generation to match Prisma's @default(cuid()) used elsewhere — keeps
# IDs minted by the sidecar consistent with rows minted by NestJS/Prisma.
cuid==0.4

# v0 baseline anomaly detector (IsolationForest) — light footprint, no GPU.
# Plan-of-record graduates to LSTM AE once we have ≥7 days × multi-pond data.
numpy==2.1.1
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2

# Future deep-model deps (uncomment when LSTM AE / TFT lands):
# torch==2.4.1
# pytorch-forecasting==1.0.0
# opacus==1.5.2
