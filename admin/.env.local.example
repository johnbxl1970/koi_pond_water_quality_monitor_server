# Admin app server-side env. Copy to .env.local; .env.local is gitignored.
# Auth uses per-user JWT login (cookie-based) — no shared bearer token.

# Where to reach the NestJS server. Default is local dev.
KOI_SERVER_URL=http://localhost:3000
